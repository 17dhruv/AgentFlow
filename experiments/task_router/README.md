# Task Router Experiment

This directory is reserved for a future lightweight ML experiment that routes incoming tasks to agent roles.

Planned approach:

- Build labeled examples from benchmark task prompts
- Start with a simple TF-IDF + logistic regression baseline
- Compare against rule-based routing
- Report routing accuracy and confusion matrix

Do not claim this model is trained until the experiment is implemented and evaluated.
